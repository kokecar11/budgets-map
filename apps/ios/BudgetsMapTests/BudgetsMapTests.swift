import XCTest
@testable import BudgetsMap

/// Placeholder test target created in Phase 0.
/// Actual test cases (stats math, KeychainStore CRUD, TokenRefresher single-flight) are added post-MVP.
final class BudgetsMapTests: XCTestCase {
    func testPlaceholder() {
        // This test exists only to keep the test target compilable.
        XCTAssertTrue(true)
    }
}
