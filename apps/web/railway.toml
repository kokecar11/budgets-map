[build]
dockerfilePath = "apps/web/Dockerfile"
buildArgs = { NEXT_PUBLIC_API_URL = "${{api.RAILWAY_PUBLIC_DOMAIN}}" }

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
